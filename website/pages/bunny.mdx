# Bunny

The Bunny daemon polls the queue for jobs and executes availability and distribution queries against the database.
A result filtered by the obfuscation as configured for your instance is posted to [Relay](/relay).
