## Core API reference
The scripts in `core` are called by the Resolve daemon and command-line interface (CLI).

The classes used by the core functions are held in [rquest-dto](/resolve/core_api_ref/rquest-dto).
